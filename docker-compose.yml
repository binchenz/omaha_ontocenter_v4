services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: omaha
      POSTGRES_PASSWORD: omaha
      POSTGRES_DB: ontocenter
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
